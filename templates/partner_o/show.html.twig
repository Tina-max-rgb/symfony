{% extends 'base.html.twig' %}

{% block title %}PartnerO{% endblock %}

{% block body %}
    <h1>PartnerO</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner_o.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ partner_o.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ partner_o.email }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ partner_o.userId }}</td>
            </tr>
            <tr>
                <th>Is_active</th>
                <td>{{ partner_o.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Id_permission</th>
                <td>{{ partner_o.idPermission }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_partner_o_index') }}">back to list</a>

    <a href="{{ path('app_partner_o_edit', {'id': partner_o.id}) }}">edit</a>

    {{ include('partner_o/_delete_form.html.twig') }}
{% endblock %}
