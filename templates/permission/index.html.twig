{% extends 'base.html.twig' %}

{% block title %}Permission index{% endblock %}

{% block body %}
    <h1>Permission index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Partenaire_id</th>
                <th>Structure_id</th>
                <th>Is_active</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for permission in permissions %}
            <tr>
                <td>{{ permission.id }}</td>
                <td>{{ permission.nom }}</td>
                <td>{{ permission.partenaireId }}</td>
                <td>{{ permission.structureId }}</td>
                <td>{{ permission.isActive ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_permission_show', {'id': permission.id}) }}">show</a>
                  
                    <a href="{{ path('app_permission_edit', {'id': permission.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_permission_new') }}">Create new</a>
{% endblock %}
